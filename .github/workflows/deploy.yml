name: Deploy to VPS

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull and build
        run: |
          cd /opt/situationroom-v3
          git pull
          npm run build
          sudo pm2 restart situationroom-v3
